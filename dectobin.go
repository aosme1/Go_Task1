package main

import "fmt"

func main() {

	var x, y, z int = 30, 42, 53

	fmt.Printf("x = %d, in binary format =%b\n", x, x)
	fmt.Printf("y = %d, in binary format =%b\n", y, y)
	fmt.Printf("z = %d, in binary format =%b\n", z, z)
}
